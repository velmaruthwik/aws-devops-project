pipeline {
    agent any

    stages {
        stage('Checkout Validation') {
            steps {
                echo 'Repository cloned successfully'
                sh 'ls -la'
            }
        }

        stage('Build') {
            steps {
                echo 'Build successful'
            }
        }
    }
}
