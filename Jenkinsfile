pipeline {
    agent any

**  stages {
        stage('Checkou**Validation') {
            steps **                echo 'Repository **oned successfully'
              **sh 'ls -la'
            }
       **

        stage('Build') {
      **    steps {
                echo **uild successful'
            }
  **    }
    }
}
